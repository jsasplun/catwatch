# Training, evaluation, export, labeling, and quality tools.
# Exact versions keep runs reproducible. Upgrade deliberately, one line at a
# time, and re-run `make verify` afterwards.
torch==2.5.1
torchvision==0.20.1
onnx==1.17.0
onnxruntime==1.20.1
opencv-python==4.10.0.84
numpy==1.26.4
scikit-learn==1.5.2
PyYAML==6.0.2
python-dotenv==1.0.1
dvc==3.56.0

ruff==0.7.4
pyright==1.1.389
pytest==8.3.3