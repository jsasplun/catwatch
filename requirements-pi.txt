# Raspberry Pi only. First install these with apt, NOT pip:
#   sudo apt install -y python3-picamera2 python3-opencv python3-numpy
# picamera2's compiled parts are built against the system NumPy; a pip-installed
# NumPy can break it. Then, inside a venv created with --system-site-packages:
onnxruntime==1.20.1
PyYAML==6.0.2
python-dotenv==1.0.1